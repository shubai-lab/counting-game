# 预流失召回链路优化 · 流程图 v1.0

> 本流程图对应方案A（扩大推送覆盖）+ 方案C（简化任务+武将体验卡激励）

---

## 1. 主流程：预流失召回链路

```mermaid
flowchart TD
    subgraph 圈选阶段["一、圈选阶段"]
        A["数据系统标记预流失用户"] --> B["生成预流失用户名单"]
        B --> C["每周/每期滚动执行"]
    end

    subgraph 触达阶段["二、触达阶段 - 方案A"]
        C --> D["接入APP推送通道"]
        D --> E{用户设备在线?}
        E -->|是| F["推送触达用户"]
        E -->|否| G["等待用户上线后再触达"]
        F --> H["用户打开APP"]
        G --> H
    end

    subgraph 登录领取["三、登录和领取武将卡 - 方案C"]
        H --> I["用户登录成功"]
        I --> J["进入活动界面"]
        J --> K["系统判断当前为第几天"]
        K --> L{Day1登录?}
        L -->|是| M["领取武将试用卡1"]
        L -->|否| N{Day2登录?}
        N -->|是| O["领取武将试用卡2"]
        N -->|否| P{Day3登录?}
        P -->|是| Q["领取武将试用卡3"]
        P -->|否| R["已是最后一天或异常"]
        M --> S["武将体验卡到账，有效期7天"]
        O --> S
        Q --> S
        S --> T["武将体验卡可在任意时间使用"]
    end

    subgraph 任务完成["四、任务完成 - 方案C"]
        T --> U["用户进入任意对局"]
        U --> V{PVP还是PVE?}
        V -->|PVE| W["首局100%出现武将体验卡弹窗"]
        V -->|PVP| X["无武将体验卡弹窗"]
        W --> Y["用户使用武将体验卡完成一局"]
        X --> Z["仍算完成任意对局1次"]
        Y --> Z
        Z --> AA["任务完成计数+1"]
        AA --> AB["用户回到活动界面"]
        AB --> AC["手动领取牌局奖励"]
        AC --> AD["牌局奖励到账"]
    end

    subgraph 次留判断["五、次留判断"]
        AD --> AE{次日用户是否登录?}
        AE -->|是| AF["次留成功"]
        AE -->|否| AG["用户流失"]
    end

    style A fill:#fef3c7,stroke:#d97706
    style H fill:#dbeafe,stroke:#2563eb
    style M fill:#d1fae5,stroke:#059669
    style Y fill:#d1fae5,stroke:#059669
    style AC fill:#d1fae5,stroke:#059669
    style AF fill:#059669,stroke:#065f46,color:#fff
    style AG fill:#fee2e2,stroke:#dc2626
```

---

## 2. 异常分支

```mermaid
flowchart TD
    subgraph 中断处理["中断处理：Day1领卡未完成"]
        A1["Day1登录领取武将卡"] --> A2["用户未完成对局就退出"]
        A2 --> A3["武将卡仍保留在背包，有效期7天"]
        A3 --> A4["Day2用户再次登录"]
        A4 --> A5["可正常领取武将卡2"]
        A5 --> A6["不要求先完成Day1任务"]
    end

    subgraph 有效期处理["有效期处理"]
        B1["武将体验卡有效期7天"] --> B2{到期前是否使用?}
        B2 -->|未使用| B3["卡自动失效"]
        B2 -->|已使用| B4["使用后卡消失，不占用背包"]
    end

    subgraph 等级段判断["等级段判断"]
        C1["用户登录时系统读取当前等级和官阶"] --> C2["匹配对应档位奖励"]
        C2 --> C3{等级段是哪档?}
        C3 -->|60级以下| C4["界钟会 / 尹夫人 / 牛辅"]
        C3 -->|61-100级| C5["牛辅 / 赵嫣 / 王昶"]
        C3 -->|101-150级| C6["王昶 / 滕公主 / 张嫙"]
        C3 -->|151级以上| C7["郭照 / 张嫙 / 曹金玉"]
        C3 -->|国护军及以上| C8["马伶俐 / 庞凤衣 / 神庞统"]
    end

    style A3 fill:#fef9c3,stroke:#ca8a04
    style B3 fill:#fee2e2,stroke:#dc2626
    style C8 fill:#fdf4ff,stroke:#7c3aed
```

---

## 3. 流程节点说明

| 节点 | 类型 | 说明 |
|------|------|------|
| 圈选阶段 | 自动 | 数据系统按规则标记预流失用户，每周滚动执行 |
| 触达阶段 | 自动+手动 | 方案A：接入APP推送，覆盖50%用户 |
| 登录领取 | 手动 | 用户主动进入活动界面领取每日武将卡 |
| 任务完成 | 手动 | 方案C：只需完成任意对局1次（PVP/PVE均可） |
| 次留判断 | 自动 | 次日登录则视为留存用户 |

---

## 4. 关键设计决策

1. **中断友好**：Day1未完成不影响Day2领取，避免用户因一日中断而彻底流失
2. **体验卡7天有效期**：给用户灵活空间，不强制当日使用
3. **首局100%出现**：针对PVE局，增加武将体验卡的曝光和使用率
4. **全员统一任务**：不分官阶，统一"登录+任意对局1次"，降低低官阶用户参与门槛
5. **手动领取牌局奖励**：增加用户主动行为，提升参与感和仪式感

---

版本：v1.0 | 日期：2026-05-08 | 对应方案：方案A + 方案C
